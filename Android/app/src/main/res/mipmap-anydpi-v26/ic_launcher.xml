<?xml version="1.0" encoding="utf-8"?>
<!--
    KhanaBook adaptive icon — standard (square/squircle/circle masks).
    Uses the vector foreground so it scales and clips correctly on all
    launcher shapes. The PNG (khanabook_logo.png) is NOT used here
    because PNGs don't clip cleanly with adaptive icon masks.
-->
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <background android:drawable="@drawable/ic_launcher_background" />
    <foreground android:drawable="@drawable/ic_launcher_foreground" />
    <!-- monochrome layer for themed icons (Android 13+) -->
    <monochrome android:drawable="@drawable/ic_launcher_foreground" />
</adaptive-icon>
