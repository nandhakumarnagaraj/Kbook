<?xml version="1.0" encoding="utf-8"?>
<!--
    System splash icon (API 31+).
    Uses the logo mark only (no text) — text is handled by BrandedStartFrame.
    Sized to fit within the 160dp safe circle of the 240dp icon area.
    The system applies circular masking: content outside the inner 2/3 is clipped.
-->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/splash_logo"
    android:insetLeft="20%"
    android:insetTop="20%"
    android:insetRight="20%"
    android:insetBottom="20%" />
