# Prototype → Angular mapping

| Prototype element (React)                         | Suggested Angular component                                 | Inputs                                                                                              | Outputs                                | CSS variables                                                                          | Responsive behavior                           | Accessibility contract                                                |
| ------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------- | --------------------------------------------- | --------------------------------------------------------------------- |
| `AppShell` (`src/proto/AppShell.tsx`)             | `KbAppShellComponent`                                       | `role`, `user`, `activeRoute`                                                                       | `signOut`, `navigate`                  | `--kb-color-bg-app`, `--kb-color-espresso`, `--kb-sidebar-width`, `--kb-topbar-height` | Sidebar ≥ 1024px, drawer below; topbar sticky | `<nav aria-label="Primary">`, skip-link, `<main id="main">`           |
| Sidebar nav inside `AppShell`                     | `KbSidebarComponent`                                        | `role`, `activeRoute`, `entries?`                                                                   | `navigate`                             | `--kb-color-espresso`, `--kb-color-espresso-foreground`, `--kb-color-saffron`          | Hidden below 1024px                           | `aria-current="page"` on active entry                                 |
| Mobile `Drawer` for nav                           | `KbMobileDrawerComponent`                                   | `open`, `role`, `activeRoute`                                                                       | `openChange`, `navigate`               | `--kb-color-espresso`, `--kb-z-modal`, `--kb-duration-slow`                            | Only used < 1024px                            | `role="dialog"`, `aria-modal="true"`, focus trap, Escape closes       |
| Topbar inside `AppShell`                          | `KbTopbarComponent`                                         | `title`, `showHamburger`, `user`                                                                    | `toggleDrawer`, `signOut`              | `--kb-topbar-height`, `--kb-z-topbar`                                                  | Hamburger < 1024px                            | `<header>`, `aria-expanded` on hamburger                              |
| Page title block (proto pages)                    | `KbPageHeaderComponent`                                     | `title`, `description`, `breadcrumb`, `variant`                                                     | –                                      | `--kb-fs-page-title`, `--kb-fs-welcome`, `--kb-font-display`                           | Actions stack on mobile                       | Single `<h1>` per page                                                |
| `KpiCard` (`src/proto/primitives.tsx`) default    | `KbKpiCardComponent` variant `default`                      | `label`, `value`, `format`, `delta`, `sparklineData`                                                | –                                      | `--kb-color-surface`, `--kb-radius-xl`, `--kb-shadow-xs`                               | 1/2/3/4 col grid                              | Trend text + colour, never colour alone                               |
| `KpiCard` hero (OWNER only)                       | `KbKpiCardComponent` variant `hero`                         | as above + `variant="hero"`                                                                         | –                                      | `--kb-gradient-hero`, `--kb-shadow-warm`, `--kb-radius-2xl`, `--kb-fs-hero-value`      | Full-width on mobile                          | Same, plus large value in `<span aria-label>` if formatting ambiguous |
| `Badge` (`src/proto/primitives.tsx`)              | `KbStatusBadgeComponent`                                    | `tone`, `label`, `size`, `icon?`                                                                    | –                                      | Tone `-soft` + strong colour tokens                                                    | Static                                        | Label carries state; not colour-only                                  |
| `TrendChart` (proto sparkline)                    | Reuse existing Angular chart lib                            | project-defined                                                                                     | project-defined                        | project-defined                                                                        | project-defined                               | Off-screen text summary describing the trend                          |
| `StateSwitcher` (proto only)                      | **REMOVE** — do not port                                    | –                                                                                                   | –                                      | –                                                                                      | –                                             | –                                                                     |
| `Skeleton`                                        | `KbSkeletonComponent`                                       | `variant`, `width`, `height`, `count`                                                               | –                                      | `--kb-color-muted`, `--kb-color-surface-2`                                             | Unchanged                                     | `role="status"` on wrapper, individual `aria-hidden="true"`           |
| `Dialog`                                          | `KbDialogComponent`                                         | `open`, `title`, `description`, `size`, `dismissible`, `role`                                       | `openChange`, `dismiss`                | `--kb-radius-xl`, `--kb-shadow-lg`, `--kb-z-modal`                                     | Bottom sheet on mobile                        | `aria-modal="true"`, focus trap, Escape closes, focus restored        |
| `Drawer`                                          | `KbDrawerComponent`                                         | `open`, `title`, `size`                                                                             | `openChange`, `dismiss`                | `--kb-shadow-lg`, `--kb-z-modal`                                                       | Full-width slide-in on mobile                 | Same as dialog                                                        |
| Refund / delete / deactivate confirm modals       | `KbConfirmationDialogComponent`                             | `title`, `entityLabel`, `consequence`, `confirmLabel`, `tone`, `state`, `requireTypedConfirmation?` | `confirm`, `cancel`, `openChange`      | Inherits                                                                               | Bottom sheet on mobile                        | `role="alertdialog"`, default focus on Cancel                         |
| Order / staff / menu / businesses tables          | `KbDataTableComponent<T>`                                   | `columns`, `rows`, `loading`, `empty`, `error`, `density`, `trackBy`                                | `retry`, `sortChange`                  | `--kb-color-surface-2`, `--kb-radius-xl`                                               | Switch to `kb-mobile-data-card` < 768px       | `<table>`, `<th scope="col">`, `<caption>` hidden                     |
| Mobile order / staff cards                        | `KbMobileDataCardComponent`                                 | `title`, `subtitle`, `meta`, `status`, `href`                                                       | `activate`                             | `--kb-color-surface`, `--kb-radius-xl`                                                 | Only < 768px                                  | Single focusable element                                              |
| Filter row above tables                           | `KbFilterToolbarComponent`                                  | `dense?`                                                                                            | –                                      | `--kb-space-3`                                                                         | Wraps to multiple rows                        | `role="search"` if search-dominant                                    |
| Search input (proto)                              | Use `KbFormFieldComponent` + native `<input type="search">` | project-defined                                                                                     | project-defined                        | `--kb-color-input`, `--kb-color-ring`                                                  | Full-width mobile                             | Labelled input, `type="search"`                                       |
| Primary / secondary / ghost / danger buttons      | `KbButtonComponent`                                         | `variant`, `size`, `disabled`, `loading`, `iconOnly`, `ariaLabel?`                                  | `click`                                | `--kb-color-primary`, `--kb-color-primary-hover`, `--kb-color-danger`                  | ≥ 44px on mobile primary                      | Icon-only requires `ariaLabel`, `aria-busy` while loading             |
| Form field wrapper (labels/errors in proto)       | `KbFormFieldComponent`                                      | `label`, `for`, `hint`, `error`, `required`, `disabled`                                             | –                                      | `--kb-color-danger`, `--kb-color-ring`                                                 | Full-width mobile                             | `<label for>`, `aria-describedby`, `aria-invalid`                     |
| Marketplace secret input (proto masked / replace) | `KbSecretInputComponent`                                    | `id`, `hasStoredValue`, `maskedPreview`, `disabled?`                                                | `mode`, `valueChange`, `cancelReplace` | `--kb-color-border`, `--kb-color-primary`                                              | Full-width mobile                             | `aria-readonly` in stored mode, `aria-pressed` on toggle              |
| Temp password / recovery token copy               | `KbCopyButtonComponent`                                     | `value`, `label`, `copiedLabel`, `variant`, `size`, `ariaLabel?`                                    | `copied`, `copyError`                  | `--kb-color-primary`, `--kb-color-success`                                             | ≥ 40px touch target                           | Announce via live region, not by mutating name                        |
| OCR upload (proto menu page)                      | `KbFileUploadComponent`                                     | `accept`, `maxSizeBytes`, `state`, `progress`, `filename`, `errorMessage`, `partialMessage`         | `fileSelected`, `retry`, `cancel`      | `--kb-color-border-strong`, `--kb-color-primary`                                       | Full-width mobile                             | `role="progressbar"`, live region for state                           |
| Empty list placeholders                           | `KbEmptyStateComponent`                                     | `title`, `description`, `illustration`, `dense`                                                     | –                                      | `--kb-color-muted-foreground`                                                          | Reduced padding on mobile                     | Illustration `aria-hidden`                                            |
| Failed load placeholders                          | `KbErrorStateComponent`                                     | `title`, `description`, `retryLabel`, `showRetry`, `dense`                                          | `retry`                                | `--kb-color-danger`                                                                    | Reduced padding on mobile                     | `role="alert"` when appearing after a user action                     |
| Toast (proto success/error)                       | `KbToastComponent` + `KbToastService`                       | per-toast: `tone`, `title`, `description`, `duration`, `actionLabel`, `dismissible`                 | `action`, `dismiss`                    | `--kb-color-surface`, `--kb-z-toast`                                                   | Top-stack on mobile                           | `role="status"` / `role="alert"` by tone                              |
| INR display (proto `INR` helper)                  | `KbCurrencyComponent` (or `kbInrCurrency` pipe)             | `value`, `decimals`, `showSymbol`, `sign`, `size`                                                   | –                                      | `--kb-fs-kpi-value`, `--kb-fs-hero-value`, `--kb-font-display`                         | Wrap-aware                                    | `₹` is meaningful text                                                |

**Removed by design (do NOT port):**

- Prototype `StateSwitcher` floating pill.
- Any prototype route selector or mock data toggles.
- Any React-only debug helpers under `src/proto/`.
